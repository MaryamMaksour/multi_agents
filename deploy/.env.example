# Copy to deploy/.env and fill in. Docker Compose reads it automatically,
# because the compose file lives in this directory.
#
#   cp deploy/.env.example deploy/.env
#   $EDITOR deploy/.env
#   docker compose -f deploy/docker-compose.yml up -d --force-recreate
#
# Why a file rather than `export`: a container reads its environment once,
# when it is created. An export in one terminal and a `docker compose up` in
# another produce a container configured from neither - which looks exactly
# like a wrong key, a wrong model, or a wrong endpoint, and is none of them.
# A file has no such ambiguity, and `docker compose config` will show you
# what the container is actually going to get.
#
# deploy/.env is gitignored. This example file is not - keep secrets out of it.

# --- the model -----------------------------------------------------------
# Any OpenAI-compatible endpoint. Keys are usually region-bound: a DashScope
# key made in the Beijing console returns 401 on the Singapore host and vice
# versa, and the message blames the key rather than the region.
#
#   Singapore  https://dashscope-intl.aliyuncs.com/compatible-mode/v1
#   Beijing    https://dashscope.aliyuncs.com/compatible-mode/v1
#   local      http://host.docker.internal:11434/v1        (Ollama)
#   scripted   http://host.docker.internal:11435/v1        (scripts/demo_model.py)
QWEN_API_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1
QWEN_API_KEY=
QWEN_MODEL=qwen-plus
# qwen3-* hybrid-thinking models need enable_thinking=false for non-streaming
# calls; strict OpenAI-compatible endpoints reject the field. Leave empty to
# infer from QWEN_MODEL (qwen3-* -> false, others -> not sent). true/false
# force it; none forces it off.
QWEN_ENABLE_THINKING=

# Anything else the provider wants on the request body, as a JSON object. The
# escape hatch that keeps a one-parameter provider quirk from meaning an
# adapter edit and a rebuild. Merged over the enable_thinking value above, so
# it can override it. Malformed JSON, or JSON that is not an object, fails at
# startup next to this variable rather than inside the first question:
#
#   QWEN_EXTRA_BODY={"top_k": 20, "repetition_penalty": 1.05}
QWEN_EXTRA_BODY=

# Check a model before committing to it. It reports both whether the key may
# call it and whether it returns a tool call at all - a model that answers in
# prose cannot drive this system and fails silently:
#
#   python3 scripts/check_model.py qwen-plus --embedding text-embedding-v3

# --- embeddings ----------------------------------------------------------
# Must return EMBEDDING_DIM values. The schema fixes that as vector(1024), so
# a different width is a migration and a re-embedding of every row.
QWEN_EMBED_MODEL=text-embedding-v3
EMBEDDING_DIM=1024

# Leave empty to use the chat endpoint above. Set them to run embeddings
# somewhere else entirely - a second server, or another provider whose API is
# OpenAI-compatible:
#
#   EMBED_API_URL=https://api.cohere.ai/compatibility/v1
#   QWEN_EMBED_MODEL=embed-multilingual-v3.0
EMBED_API_URL=
EMBED_API_KEY=

# --- the database --------------------------------------------------------
# app_authenticator is NOINHERIT and holds nothing on its own; each pool runs
# SET ROLE to become the agent it serves. See seeds/003_roles.sql.
PG_USER=app_authenticator
PG_PASSWORD=dev_authenticator

# --- pgvector ------------------------------------------------------------
# '<=>' cosine, '<->' L2, '<#>' inner product. Interpolated into SQL, so it is
# validated at startup rather than parameterised - and it reaches the model's
# filter guidance too, so the operator it is told to write is the one the
# index was built for.
DIST_OP=<=>

# --- logging -------------------------------------------------------------
# Every record passes through a formatter that removes the configured secret
# values from the message, the arguments, the extra fields and the traceback
# - the last being the one that matters, since nobody writes logger.info(key)
# but an exception carries the request that raised it.
#
# INFO already carries every tool call with its arguments in full, which is
# the single most useful line in the system: "how many Arabic novels under 300
# pages" arriving as a query with no genre filter is what separates a bad
# delegation from a bad sub-agent.
LOG_LEVEL=INFO
# text for a person, json for a collector.
LOG_FORMAT=text

# --- limits --------------------------------------------------------------
# One question is an embedding call plus several model calls priced per token,
# so its length is a cost limit. Over it is a 422, before anything is spent.
MAX_QUESTION_CHARS=4000

# How many model calls one turn may make before it has to answer with what it
# has. Without a budget the loop runs until LangGraph's own recursion limit
# raises - after paying for every call up to it.
AGENT_MAX_STEPS=12

# Whether a 500 names the exception that caused it. true is right for
# development, where "CacheError: Extra data: line 1 column 9" is the whole
# diagnosis; false for a deployment reachable by people who are not operating
# it, where the same sentence is a description of the internals. The log line
# is identical either way.
EXPOSE_ERRORS=true
