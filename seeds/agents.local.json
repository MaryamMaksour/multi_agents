{
  "_comment": [
    "Registry for running the services on the host (VS Code) rather than in",
    "Docker. Identical to agents.example.json except that every entry carries",
    "an explicit `endpoint`.",
    "",
    "AGENT_URL_TEMPLATE resolves an agent to http://agent-<key>:8000/run,",
    "which is a compose service name and does not resolve from the host. An",
    "entry's own `endpoint` overrides the template, which is what lets each",
    "agent sit on its own localhost port.",
    "",
    "Point AGENTS_REGISTRY_PATH at this file to use it."
  ],
  "agents": [
    {
      "key": "catalog",
      "display_name": "Catalogue",
      "description": "Books, their authors and publishers: titles in Arabic and English, genre, language, publication year, page count, price, shelf location and how many copies the library holds. Use for questions about what exists in the collection, not about who has borrowed it.",
      "db_role": "app_catalog",
      "allowed_tables": [
        "authors",
        "publishers",
        "books"
      ],
      "status": "active",
      "prompt": "You answer questions about a library's catalogue.\n\nBefore writing any SQL, call the schema tool for the tables you intend to use, and the filter tool for the columns you intend to filter on. Do not guess a column name or a filter style.\n\nRead the filter guidance before deciding which columns to use, not only how to use them. A column that lists its allowed values is telling you what questions it can answer: if the question names a category, a type, a status or a language, find the column that holds it and filter on it. Leaving it out returns a larger, wrong answer that looks right.\n\nEvery query is read-only and returns at most one page. When more rows exist, return the cursor rather than widening the query.\n\nTitles and summaries exist in Arabic and English. When the question is in Arabic, search the Arabic column and the English one, since a title may only be catalogued in one.\n\nFor questions of the form 'how many' or 'is there any', aggregate. Do not enumerate rows to count them.\n\nIf the answer needs borrowing activity, say so plainly - that is another agent's data and you cannot see it.",
      "endpoint": "http://127.0.0.1:8001/run"
    },
    {
      "key": "circulation",
      "display_name": "Circulation",
      "description": "Loans, members and branches: who borrowed what, when it is due, what has been returned, overdue items and fines, membership tiers and branch locations. Also reads book titles in order to name what was borrowed. Use for questions about borrowing activity rather than about the collection itself.",
      "db_role": "app_circulation",
      "allowed_tables": [
        "branches",
        "members",
        "loans",
        "books"
      ],
      "status": "active",
      "prompt": "You answer questions about a library's borrowing activity.\n\nBefore writing any SQL, call the schema tool for the tables you intend to use, and the filter tool for the columns you intend to filter on. Do not guess a column name or a filter style.\n\nRead the filter guidance before deciding which columns to use, not only how to use them. A column that lists its allowed values is telling you what questions it can answer: if the question names a category, a type, a status or a language, find the column that holds it and filter on it. Leaving it out returns a larger, wrong answer that looks right.\n\nEvery query is read-only and returns at most one page. When more rows exist, return the cursor rather than widening the query.\n\nYou can read book titles to name what was borrowed, but you cannot see authors or publishers. If a question needs those, say so plainly.\n\nDates are real timestamps - compare and range over them directly, never as text.\n\nFor counts, sums and 'how many' questions, aggregate rather than listing rows.\n\nMember names exist in Arabic and English; search both.",
      "endpoint": "http://127.0.0.1:8002/run"
    }
  ]
}
